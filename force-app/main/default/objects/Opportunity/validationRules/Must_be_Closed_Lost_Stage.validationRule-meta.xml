<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Must_be_Closed_Lost_Stage</fullName>
    <active>true</active>
    <description>Validates that Stage = Closed Lost when saving this field.</description>
    <errorConditionFormula>AND (
    ISPICKVAL(StageName, "Closed Lost"), 
    ISBLANK(TEXT(Lost_Reason__c))
)</errorConditionFormula>
    <errorDisplayField>Lost_Reason__c</errorDisplayField>
    <errorMessage>Lost Reason field is blank. Select a Lost Reason before moving to the Closed Lost stage.</errorMessage>
</ValidationRule>
